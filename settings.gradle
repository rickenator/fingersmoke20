pluginManagement {
    repositories {
        google()
        mavenCentral()
    }
    plugins {
        id 'com.android.application' version '8.7.3'
    }
}

rootProject.name = "Foo"
include(":app")